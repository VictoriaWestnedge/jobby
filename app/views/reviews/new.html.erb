<div class="container">
  <div class="row mt-3 mb-4">
    <div class="col">
      <%= simple_form_for([@user, @review]) do |f| %>
        <%= f.input :description %>
        <%= f.input :rating_star %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>
